---
import Marker from "@client/components/Marker.astro";
import { ContactForm } from "@client/components/contact/ContactForm.tsx";
import BaseLayout from "@client/layouts/BaseLayout.astro";
import { styled as p } from "panda/jsx";
import { CopyWrapper } from "src/components/CopyWrapper";
import { INFO } from "src/lib/config";
---

<BaseLayout
  header={{
    bgColor: "colors.9u-white",
    navAndLogoColor: "colors.9u-brown",
  }}
  seo={{
    title: "Contact",
  }}
  style={{
    bgColor: "colors.9u-white",
  }}
>
  <p.div m="0 auto" w="100%">
    <Marker>
      <p.h1>Contact</p.h1>
    </Marker>
  </p.div>

  <p.article py="10" w="100%">
    <p.p textAlign="center">
      お問い合わせ内容をこちらからお送りください。<br />
      後日、こちらからご連絡さしあげますので、{" "}
      <CopyWrapper
        client:load
        copyText={INFO.addr.email.contact}
        titleText="メールアドレスをコピー"
      >
        {INFO.addr.email.contact}
      </CopyWrapper>{" "}
      からのメールを受信できるように設定をお願いいたします。
    </p.p><br />
    <ContactForm client:load />
  </p.article>
</BaseLayout>
